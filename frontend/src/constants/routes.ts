export enum RoutePaths {
    MONTHLY = "/",
    CHARTS = "/charts",
    LABELS = "/labels",
}

export enum RouteKeys {
    MONTHLY = "monthly-table",
    CHARTS = "charts",
    LABELS = "labels",
}
